# 邀请码卡片设计 QA

- 参考图：`artifacts/invite-pill-controls-reference.png`
- 实现截图：`artifacts/invite-unified-controls-implementation.png`
- 并排对比：`artifacts/invite-pill-controls-comparison.png`
- 验证视口：`1280 × 720`
- 验证状态：邀请人待绑定，邀请码与复制链接可见

## 对照结果

- 邀请人和复制链接区域统一使用评论输入框的外层胶囊容器，均为 `46px` 高、`999px` 圆角。
- 两个外层控件均为 `360 × 46px`，两个按钮均为 `72 × 34px`，边框均为 `1px`。
- 输入提示和邀请链接统一为 `13px / 300`、`rgba(255,255,255,.62)`。
- 「绑定」按钮继续使用 `UIguide.html` 次要按钮语义，「复制」保留金色主操作语义。
- 卡片使用 24px 圆角、低对比金色描边、深色渐变表面和细分隔线。
- 邀请码使用大号金色字，浅色品牌 Logo 与 `SG2026` 同行显示，间距为 `24px`。
- 说明文字与一体式链接栏的层级、间距和对比度已对齐参考图。
- 复制按钮文案缩短为「复制」，字重与颜色和「立即邀请好友」一致。
- 邀请码卡片高度缩减至约 `340px`，与左侧约 `307px` 高的文案区域更平衡。
- 背景替换为 `assets/invite/inviteBG.png`。
- 宇航员放大至约 `435px` 宽的资源框，并保持在邀请码卡片右侧。
- 「邀请奖励规则」标题保留在卡片内部，外层、四张规则卡片和积分标签的描边全部移除。
- 页面无横向溢出；左侧邀请文案起点由 `24px` 调整为 `56px`。
- 左侧绿色邀请标签已删除；邀请人区域顶部留白增加，下方分割线已移除。

## 交互与运行检查

- 邀请人输入框默认可编辑，显示邀请码输入提示。
- 使用 `ABCD1234` 验证绑定流程后，表单正确进入 `bound` 状态、锁定输入框并显示成功反馈。
- 绑定按钮成功态显示单行「已绑定」，没有文字换行。
- 复制按钮保留现有复制反馈逻辑。
- 邀请码卡片水印资源加载成功。
- HTML/CSS 变更通过局部 `git diff --check`，样式表花括号配对正常。

final result: passed
